# POC-12A Checkpoint

POC-12A is GREEN when the proving course loads as one ordered course definition, rejects mismatched or out-of-order hole membership, and derives par and tee yardage totals from its hole definitions.
